[project]
name = "autofaith"
version = "0.1.0"
description = "AutoFaith project"
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Your Name" }
]
dependencies = [
    "pantograph",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.uv.sources]
pantograph = { git = "https://github.com/stanford-centaur/PyPantograph" }
